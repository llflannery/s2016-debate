import { ProjectReport, Dependency } from '../interfaces';
export declare class CacheBuilder {
    project: ProjectReport;
    constructor(project: ProjectReport);
    buildCache(): Promise<void>;
    static buildDepCache(deps: Array<Dependency>, buildDir: string): Promise<void>;
    static createCache(deps: Array<Dependency>, cacheDir: string): Promise<{}>;
}
