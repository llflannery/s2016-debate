import { Options } from "node-sass";
import { ProjectReport } from "../interfaces/ProjectReport";
export declare class SassCompiler {
    options: {
        includePaths: any[];
        outputStyle: string;
        sourceComments: boolean;
        sourceMapEmbed: boolean;
    };
    constructor(options?: {
        includePaths: any[];
        outputStyle: string;
        sourceComments: boolean;
        sourceMapEmbed: boolean;
    });
    compile(report: ProjectReport, bits: any): Promise<{
        globals: string;
        bits: string;
    }>;
    static compileBits(report: ProjectReport, options: Options, bits: any): Promise<string>;
    static compileGlobals(report: ProjectReport, options: Options): Promise<string>;
    static renderFile(options: any, filePath: any): Promise<{}>;
}
