import { CompiledPage, ProjectReport } from "../interfaces";
export declare class FileSystemDeployer {
    deployDir: string;
    constructor(deployDir: string);
    deploy(project: {
        report: ProjectReport;
        compiledPage: CompiledPage;
    }): Promise<void>;
}
